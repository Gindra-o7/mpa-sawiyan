---
import { Image as ImageIcon } from "lucide-astro";
---

<div class="flex justify-center">
  <button
    class="group relative inline-flex items-center gap-2 px-8 py-3 bg-gradient-to-r from-[#388E3C] to-[#81D4FA] text-white font-semibold rounded-full shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-105 overflow-hidden"
    id="load-more-btn"
  >
    <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
    <ImageIcon class="w-5 h-5 relative z-10" />
    <span class="relative z-10">Muat Lebih Banyak</span>
  </button>
</div>

<script>
  // Load more functionality (placeholder for future implementation)
  const loadMoreBtn = document.getElementById('load-more-btn');
  if (loadMoreBtn) {
    loadMoreBtn.addEventListener('click', () => {
      // TODO: Implement load more functionality
      console.log('Load more clicked');
    });
  }
</script>

